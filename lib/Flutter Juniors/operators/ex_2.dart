// ignore_for_file: avoid_print

//Create two variables to store the length and width of a rectangle.
// Calculate its perimeter using the variables and print the result.

void main() {
  double length = 5;
  double width = 3;
  double perimeter = 2 * (length + width);
  print("Perimeter of rectangle: $perimeter");
}
