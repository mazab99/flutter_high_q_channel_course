// ignore_for_file: avoid_print

void main() {
  // Question: What does the `isEmpty` property check for in Dart strings?
  // Answer: The `isEmpty` property in Dart strings checks if a string has
  // no characters.

  String emptyString = "";
  bool isEmpty = emptyString.isEmpty; // true
  print("isEmpty: $isEmpty");
}
