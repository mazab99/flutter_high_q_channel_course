// ignore_for_file: avoid_print

void main() {
  // Question: How can you replace all occurrences of a substring in a string in Dart?
  // Answer: You can replace all occurrences of a substring in a string in Dart using the `replaceAll` method.

  String myString = "banana";
  String newString = myString.replaceAll("a", "i"); // "binini"
  print("replaceAll: $newString");
}
