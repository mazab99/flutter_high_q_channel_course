// ignore_for_file: avoid_print

// Create an empty list and then add integers from 1 to 5 to the list. Print the list." Use statements".

void main() {
  List<int> numbers = [];
  for (int i = 1; i <= 5; i++) {
    numbers.add(i);
  }
  print(numbers);
}
